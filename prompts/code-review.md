# Code Review Prompt

Review 我在 tft-agent-set18 里写的这段 Agent 代码。

我的目标是最小实现 + 真正理解，所以请按以下顺序提意见：

1. **正确性**：循环终止条件、状态更新顺序、tool 结果回填、异常路径
2. **契约**：LLM 接口是否可替换（真实 API / mock / 其他 provider）
3. **可观测性**：能否从外部看出 Agent 卡在哪一步
4. **失败模式**：429、超时、非法 tool 参数、模型返回空内容、无限重复调用
5. **过度设计**：哪些抽象现在不必要，应该删掉

请不要把我的代码改成"框架风格"。
如果某处应该改用 LangGraph，请明确说明：
它解决的是我这里的哪个具体痛点，以及我自己写的版本会缺什么。

只 Review 我贴出的部分，不要推测未贴出的代码。
